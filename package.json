{
  "name": "@f/obj-functor",
  "description": "Object functor.",
  "repository": "git://github.com/micro-js/obj-functor.git",
  "version": "0.1.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/assign": "^1.2.1",
    "@f/map-obj": "^1.2.2"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
